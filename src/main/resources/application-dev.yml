server:
  port: 8000

spring:
  datasource:
    username: root
    password: secret
    url: jdbc:mysql://localhost:3306/sicei?zeroDateTimeBehavior=convertToNull&serverTimezone=America/Mexico_City
  jpa:
    hibernate:
      ddl-auto: update

cors:
  origins: "*"